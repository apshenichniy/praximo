{
  "name": "@praximo/bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist src/index.ts --name praximo-bot --compatibility-date 2026-07-19 --compatibility-flags nodejs_compat",
    "check": "tsc --noEmit && oxlint -c ../../.oxlintrc.json src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@praximo/auth": "workspace:*",
    "@praximo/db": "workspace:*",
    "@praximo/domain": "workspace:*",
    "@praximo/i18n": "workspace:*",
    "@praximo/telegram": "workspace:*",
    "effect": "catalog:",
    "grammy": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@effect/vitest": "catalog:",
    "@praximo/tooling": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
